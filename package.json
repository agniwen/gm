{
  "name": "gm",
  "version": "0.1.0",
  "module": "index.ts",
  "bin": {
    "gm": "./dist/gm"
  },
  "scripts": {
    "gm": "bun ./index.ts",
    "build": "bun build ./index.ts --compile --outfile ./dist/gm && rm -f ./*.bun-build .*.bun-build *.bun-build"
  },
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@commitlint/config-conventional": "^20.4.2",
    "@commitlint/lint": "^20.4.2",
    "openai": "^6.24.0"
  }
}
